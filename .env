# 라우터에 전달할 basename
REACT_APP_BASE_NAME='/'

# 라우터 타입 설정 (hash, browser)
# browser 타입 사용시 basename, package.json homepage, public/.htaccess 에서 실제 사용할 경로를 설정해야 함
# hash 타입 사용시 basename 설정 불필요
REACT_APP_ROUTER_TYPE='hash'


# 시안 여부
REACT_APP_IS_SIAN='true'